This file contains instructions for fixing the form validation display in AddProperty.jsx:

1. Update class names for error highlighting (add conditional classes):
   - title input: add `${errors.title ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - description textarea: add `${errors.description ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - price input: Already has error handling ✓
   - street input: add `${errors.street ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - locality input: add `${errors.locality ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - city input: add `${errors.city ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - state input: add `${errors.state ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - pincode input: add `${errors.pincode ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - phone input: add `${errors.phone ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - campusName select: add `${errors.campusName ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - totalRooms input: add `${errors.totalRooms ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`
   - availableRooms input: add `${errors.availableRooms ? 'border-red-500 bg-red-50' : 'border-neutral-200'}`

2. Add error message display after each input field (pattern):
```jsx
{errors.FIELD_NAME && (
    <p className="text-red-500 text-sm font-medium mt-1.5 flex items-center gap-1">
        <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
            <path fillRule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clipRule="evenodd" />
        </svg>
        {errors.FIELD_NAME}
    </p>
)}
```

3. Special handling:
   - description: Already shows character count, wrap in conditional to show error instead
   - phone: Already shows digit count, wrap in conditional to show error instead
   - images: Update warning box to show error styling when errors.images exists
   - amenities: Add error message in the header section
